import styled from '@emotion/styled'

const ItemContainer = styled.div`
  &:first-child {
    margin-top: .125em;
  }
  &:last-child {
    margin-bottom: .125em;
  }
`

export default ItemContainer
